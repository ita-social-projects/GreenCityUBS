<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="delete-big-order-table-view" author="Kizerov Dmytro">
        <dropView viewName="big_order_table"/>
    </changeSet>

    <changeSet id="update-column-lengths" author="Kizerov Dmytro">
        <modifyDataType tableName="order_address" columnName="district" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="city" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="region" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="street" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="district_en" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="city_en" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="region_en" newDataType="VARCHAR(255)"/>
        <modifyDataType tableName="order_address" columnName="street_en" newDataType="VARCHAR(255)"/>
    </changeSet>

    <changeSet id="create-big-order-table-view" author="Kizerov Dmytro">
        <createView viewName="big_order_table" replaceIfExists="true">
            WITH order_sums AS (SELECT obm.order_id,
                                       SUM(obm.confirmed_quantity) AS total_confirmed_quantity,
                                       SUM(obm.exported_quantity)  AS total_exported_quantity,
                                       SUM(obm.amount)             AS total_amount
                                FROM order_bag_mapping obm
                                GROUP BY obm.order_id),
                 group_capacity AS (SELECT obm.order_id,
                                           SUM(CASE
                                                   WHEN bag.capacity = 120 AND bag.name_eng = 'Mix waste' THEN
                                                       CASE
                                                           WHEN os.total_confirmed_quantity IS NULL AND
                                                                os.total_exported_quantity IS NULL THEN obm.amount
                                                           WHEN os.total_exported_quantity IS NULL
                                                               THEN obm.confirmed_quantity
                                                           ELSE obm.exported_quantity
                                                           END
                                                   ELSE 0
                                               END) AS mixed_waste_120,
                                           SUM(CASE
                                                   WHEN bag.capacity = 60 AND bag.name_eng = 'Textile waste' THEN
                                                       CASE
                                                           WHEN os.total_confirmed_quantity IS NULL AND
                                                                os.total_exported_quantity IS NULL THEN obm.amount
                                                           WHEN os.total_exported_quantity IS NULL
                                                               THEN obm.confirmed_quantity
                                                           ELSE obm.exported_quantity
                                                           END
                                                   ELSE 0
                                               END) AS textile_waste_60,
                                           SUM(CASE
                                                   WHEN bag.capacity = 20 AND bag.name_eng = 'Textile waste' THEN
                                                       CASE
                                                           WHEN os.total_confirmed_quantity IS NULL AND
                                                                os.total_exported_quantity IS NULL THEN obm.amount
                                                           WHEN os.total_exported_quantity IS NULL
                                                               THEN obm.confirmed_quantity
                                                           ELSE obm.exported_quantity
                                                           END
                                                   ELSE 0
                                               END) AS textile_waste_20,
                                           STRING_AGG(
                                                   CASE
                                                       WHEN bag.name_eng NOT IN ('Mix waste', 'Textile waste') THEN
                                                           CASE
                                                               WHEN os.total_confirmed_quantity IS NULL AND
                                                                    os.total_exported_quantity IS NULL THEN
                                                                   CASE
                                                                       WHEN obm.amount = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name, ' ', bag.capacity, 'л - ', obm.amount, 'шт') END
                                                               WHEN os.total_exported_quantity IS NULL THEN
                                                                   CASE
                                                                       WHEN obm.confirmed_quantity = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name, ' ', bag.capacity, 'л - ',
                                                                                   obm.confirmed_quantity, 'шт') END
                                                               ELSE
                                                                   CASE
                                                                       WHEN obm.exported_quantity = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name, ' ', bag.capacity, 'л - ',
                                                                                   obm.exported_quantity, 'шт') END
                                                               END
                                                       ELSE NULL
                                                       END,
                                                   ', '
                                           )        AS other_packages,
                                           STRING_AGG(
                                                   CASE
                                                       WHEN bag.name_eng NOT IN ('Mix waste', 'Textile waste') THEN
                                                           CASE
                                                               WHEN os.total_confirmed_quantity IS NULL AND
                                                                    os.total_exported_quantity IS NULL THEN
                                                                   CASE
                                                                       WHEN obm.amount = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name_eng, ' ', bag.capacity, 'l - ', obm.amount, 'pcs') END
                                                               WHEN os.total_exported_quantity IS NULL THEN
                                                                   CASE
                                                                       WHEN obm.confirmed_quantity = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name_eng, ' ', bag.capacity,
                                                                                   'l - ',
                                                                                   obm.confirmed_quantity, 'pcs') END
                                                               ELSE
                                                                   CASE
                                                                       WHEN obm.exported_quantity = 0 THEN NULL
                                                                       ELSE CONCAT(bag.name_eng, ' ', bag.capacity,
                                                                                   'l - ',
                                                                                   obm.exported_quantity, 'pcs') END
                                                               END
                                                       ELSE NULL
                                                       END,
                                                   ', '
                                           )        AS other_packages_eng
                                    FROM order_bag_mapping obm
                                             LEFT JOIN bag ON obm.bag_id = bag.id
                                             LEFT JOIN order_sums os ON obm.order_id = os.order_id
                                    GROUP BY obm.order_id
                                    ORDER BY obm.order_id)
            SELECT o.id                                                      AS id,
                   o.order_status                                            AS order_status,
                   o.order_payment_status                                    AS order_payment_status,
                   CAST(o.order_date AS DATE)                                AS order_date,
                   CAST((SELECT MAX(CAST(p.settlement_date AS DATE))
                         FROM payment p
                         WHERE p.order_id = o.id
                           AND o.order_payment_status = 'PAID') AS DATE)     AS payment_date,
                   CONCAT_WS(' ', uu.first_name, uu.last_name)               AS client_name,
                   uu.phone_number                                           AS client_phone_number,
                   uu.email                                                  AS client_email,
                   CONCAT_WS(' ', uu.sender_first_name, uu.sender_last_name) AS sender_name,
                   uu.sender_phone_number                                    AS sender_phone,
                   uu.sender_email                                           AS sender_email,
                   u.violations                                              AS violations_amount,
                   a.region                                                  AS region,
                   a.city                                                    AS city,
                   a.district                                                AS district,
                   CONCAT_WS(', ',
                             CONCAT(a.street, ' ', a.house_number),
                             CASE
                                 WHEN a.house_corpus IS NOT NULL AND a.house_corpus != '' THEN CONCAT('корп.', a.house_corpus)
                                 ELSE 'корп.- ' END,
                             CASE
                                 WHEN a.entrance_number IS NOT NULL AND a.entrance_number != '' THEN CONCAT('п.', a.entrance_number)
                                 ELSE 'п.- ' END
                   )                                                         AS address,
                   a.address_comment                                         AS comment_to_address_for_client,
                   gc.mixed_waste_120,
                   gc.textile_waste_60,
                   gc.textile_waste_20,
                   gc.other_packages,
                   gc.other_packages_eng,
                   o.sum_total_amount_without_discounts                      AS total_order_sum,
                   (select string_agg(c.code, ', ')
                    from certificate c
                    where c.order_id = o.id)                                 as order_certificate_code,
                   cast(coalesce((select sum(c.points) from certificate c where c.order_id = o.id), 0)
                       +
                        coalesce(o.points_to_use, 0) as bigint)              as general_discount,
                   cast(coalesce(o.sum_total_amount_without_discounts, 0)
                       - (coalesce((select sum(p.amount)
                                    from payment p
                                    where p.order_id = o.id
                                      and p.payment_status = 'PAID'), 0)
                           + (coalesce((select sum(c.points) from certificate c where c.order_id = o.id), 0) * 100
                               + coalesce(o.points_to_use, 0) *
                                 100)) as bigint)                            as amount_due,
                   o.comment                                                 as comment_for_order_by_client,
                   cast(coalesce((select sum(p.amount)
                                  from payment p
                                  where p.order_id = o.id
                                    and p.payment_status = 'PAID'), 0)
                       + (coalesce((select sum(c.points) from certificate c where c.order_id = o.id), 0) * 100 +
                          coalesce(o.points_to_use, 0) *
                          100) as bigint)                                    as total_payment,
                   o.date_of_export                                          as date_of_export,
                   concat_ws('-', cast(o.deliver_from as time),
                             cast(o.deliver_to as time))                     as time_of_export,
                   (select string_agg(oe.additional_order, '; ')
                    from order_additional oe
                    where oe.orders_id = o.id)                               as id_order_from_shop,
                   r.name                                                    as receiving_station,
                   r.id                                                      as receiving_station_id,

                   (select concat_ws(', ', e.first_name, e.last_name)
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 3)                               as responsible_logic_man,
                   (select e.id
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 3)                               as responsible_logic_man_id,

                   (select concat_ws(', ', e.first_name, e.last_name)
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 5)                               as responsible_driver,
                   (select e.id
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 5)                               as responsible_driver_id,

                   (select concat_ws(', ', e.first_name, e.last_name)
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 2)                               as responsible_caller,
                   (select e.id
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 2)                               as responsible_caller_id,

                   (select concat_ws(', ', e.first_name, e.last_name)
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 4)                               as responsible_navigator,
                   (select e.id
                    from employee_order_position eop
                             left join employees e on eop.employee_id = e.id
                    where order_id = o.id
                      and eop.position_id = 4)                               as responsible_navigator_id,
                   o.blocked                                                 as is_blocked,
                   concat_ws(', ', e.first_name, e.last_name)                as blocked_by,
                   a.region_en                                               as region_en,
                   a.city_en                                                 as city_en,
                   a.district_en                                             as district_en,
                   a.region_id                                               as region_id,
                   a.city_id                                                 as city_id,
                   a.district_id                                             as district_id,
                   concat_ws(', ', concat(a.street_en, ' ', a.house_number),
                             case
                                 when (a.house_corpus) is not null and a.house_corpus != '' then concat('b.', a.house_corpus)
                                 else 'b.- ' end,
                             case
                                 when (a.entrance_number) is not null and a.entrance_number != '' then concat('e.', a.entrance_number)
                                 else 'e.- ' end)
                                                                             as address_en,
                   o.tariffs_info_id                                         as tariffs_info_id,
                   o.admin_comment                                           as comment_for_order_by_admin

            from orders o
                     left join group_capacity gc ON o.id = gc.order_id
                     left join employees e on o.employee_id = e.id
                     left join receiving_stations r on o.receiving_station_id = r.id
                     left join ubs_user uu on o.ubs_user_id = uu.id
                     left join order_address a on uu.id = a.id
                     left join users u on o.users_id = u.id;
        </createView>
    </changeSet>
</databaseChangeLog>